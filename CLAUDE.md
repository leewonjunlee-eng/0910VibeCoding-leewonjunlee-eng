# 프로젝트 개요

개인 소개 페이지 (자기소개 / About Me). 한 페이지짜리 정적에 가까운 사이트.

## 기술 스택

- Next.js (App Router)
- TypeScript
- Tailwind CSS

## 범위

- 페이지는 1개(단일 페이지)로 구성한다. 여러 라우트/페이지를 만들지 않는다.
- 포함할 콘텐츠: 소개 글, 관심사, 링크(예: GitHub, 이메일, SNS 등) 정도.
- 그 이상의 기능(블로그, 방명록, 댓글, 인증 등)은 추가하지 않는다.

## 데이터

- 소개 페이지에 들어갈 데이터(소개 글, 관심사 목록, 링크 목록 등)는 로컬 JSON 파일로 관리한다.
- DB 연동은 지금 하지 않는다. 나중에 별도로 추가할 예정이므로, 이를 위해 미리 추상화 레이어나 스키마를 만들 필요는 없다.

## 지금 하지 않는 것 (Out of scope)

- Vercel 배포 설정
- Supabase 연동
- DB 연결
- 인증/로그인
- CMS 연동

위 항목들은 아직 요청하지 않았으므로 제안하거나 미리 준비하지 않는다.

## 코드 스타일 / 작업 원칙

- 과도한 추상화 금지. 단일 페이지 프로젝트 규모에 맞는 단순한 구조를 유지한다.
- 필요하지 않은 설정(테스트 프레임워크, 상태관리 라이브러리, CI 등)은 요청 전까지 추가하지 않는다.
